version: '2'
services:
  jenkins:
    image: 'bitnami/jenkins:latest'
    labels:
      kompose.service.type: nodeport
    ports:
      - '8080:8080'
      - '8443:8443'
      - '50000:50000'
    volumes:
      - 'jenkins_data:/srv/jenkins'
volumes:
  jenkins_data:
    driver: local